# 전체 계획과 스펙 (2026-08-13)

로드맵의 단일 진실. 서사 근거는 [NARRATIVE.md](NARRATIVE.md), 기술 절차는 [PIPELINE.md](PIPELINE.md).

## 페이즈 개요

| 페이즈 | 내용 | 상태 |
|---|---|---|
| **P1 (지금, 재부팅 전)** | 하네스 서사 검증층 구현 · 도서 API 조사·설계 · 문서화·공개 라이선스 | 🔄 |
| **P2 (재부팅 직후)** | GPU 복구 확인 → 45초 신스펙으로 모비딕 재제작 → 구조 A/B 업로드 · 톨스토이 완성 | ⬜ |
| **P3 (P2와 병행 가능)** | **관리자 페이지** (아래 상세) · prepare_work.py (Wikipedia 자동 사실 수집) | ⬜ |
| **P4** | 도서 API 시리즈 가동 ("우리 원 소장 인기도서") · 업로드 자동화 | ⬜ |
| **P5** | 자막(내레이션과 다른 정보) · CC0 효과음 · BGM · 채널 운영 지표 루프 | ⬜ |

---

## 45초 개편 스펙 (몰입 개선)

루즈함의 원인 진단: ① TTS `-5%` 감속 낭독, ② 클립 감속 배율 최대 1.8배(슬로모션 과다),
③ 문장 길이 균일(리듬 없음), ④ 서사 무긴장(NARRATIVE.md). 넷 다 스펙으로 고정한다.

| 항목 | 기존 (30초판) | 신스펙 (45초판) |
|---|---|---|
| 총 길이 | 30±3초 | **45±3초** (60초 절대 상한 — 저작권 클레임 시 전면 차단 경계) |
| 문장/장면 수 | 6 | **8** |
| 총 글자 수 | 180~200자 | **280~310자** (6.67자/초 실측 기준) |
| 문장 길이 | 30~34자 균일 | **22~33자, 표준편차 ≥4, ≤24자 펀치라인 ≥1** |
| TTS 속도 | rate −5% | **rate 0%** (감속 제거), 문장 간 무음 0.25초 |
| 클립 감속 상한 | ~1.8배 | **≤1.5배** (81프레임 3.375초 × 1.5 = 문장 최대 5.06초 ≈ 33자 상한의 근거) |
| 어미 플랜 (8문장) | 6문장 플랜 | [합니다체, 구어체, 명사종결, 합니다체, 구어체, 명사종결, 구어체, 합니다체] |
| 서사 규칙 | 없음 | **NARRATIVE.md H1~X2 전부** (하네스 자동 검증) |
| 인용 대사 | 우연 | **정확히 1개 (S2~S6), 내부 ≤14자** — 목소리 변화 레버 |
| 모션 프롬프트 | slow/gently 다용 | **금지어: slowly·gently·drifts calmly** — 장면당 뚜렷한 동작 1개 필수 |
| 마지막 문장 | 주제 라벨 + 마무리구 | **미해결 질문 + 마무리구**, S1과 토큰 공유(루프백) |

검증 지표: 유튜브 스튜디오의 초당 리텐션 곡선 + VVSA. **v3.2(30초 구구조) vs 신판(45초
신구조)을 같은 썸네일·같은 시간대에 업로드**해 곡선을 비교한다. 좋아요가 아니라
"S5 도달률"이 판정 기준.

---

## 도서 API 시리즈 — "우리 원에 있는 인기도서"

**콘셉트**: 전국 인기대출도서를 API로 받아 → 연구원 도서관 소장 여부를 교차 확인 →
소장된 책만 영상화 → 마무리를 "이 책, 우리 도서관 서가에 있습니다"로. 시청자(원내 구성원)가
"있으니 빌려볼까"로 이어지는 구조.

### 데이터 소스 (접근성 실측 완료)

| 소스 | 역할 | 상태 |
|---|---|---|
| **도서관 정보나루** (data4library.kr) | 인기대출도서 목록 (전국/월별/연령별, ISBN 포함) | HTTP 200 확인. **무료 인증키 필요 — 회원가입 후 발급 (사용자 액션)** |
| **연구원 도서관** (library.kei.re.kr) | 소장 여부 확인 | ✅ **연동 완료.** 화면이 쓰는 Pyxis JSON API가 비로그인 개방(컬렉션 1=소장자료, 10=eBook) — `scripts/kei_holdings.py` + `GET /api/books/holdings` (24h 캐시, 제목·저자 정규화 매칭으로 출판사명 오탐 제거) |
| 알라딘 TTB / 네이버 책 | 표지·소개·평점 메타데이터 보강 | HTTP 200 확인. 키 발급 필요(선택) |
| Wikipedia action API | 고전 라인 사실 수집 | 정책 확인 완료 (UA 필수, maxlag=5, 작품당 1회 캐시) |

### 파이프라인 설계

```
[cron 주1회] 정보나루 인기 100권 (ISBN)
   → KEI 소장 교차 확인 (ISBN 매칭)
   → 신규 교집합만 works 큐에 등록 (SQLite: isbn, title, author, holdings_callno, status)
   → 관리자 페이지 승인 게이트
   → 대본 하네스 (현대서 전용 포맷) → 키프레임 → I2V → 조립
   → 마무리: "이 책은 도서관 [청구기호]에 있습니다"
```

### ⚠️ 현대서는 각색 금지 — 포맷이 다르다

고전 라인(각색·줄거리 재구성)은 퍼블릭 도메인이라 가능했지만, **현대 저작물의 줄거리
각색·재연은 2차적 저작물 침해 소지**가 있고 유튜브 "본인이 만들지 않은 자료" 정책에도 닿는다.
현대서는 줄거리를 옮기지 않고 **작품이 놓인 배경과 공기만** 그리는 소개형으로 간다.

#### 여기서 크게 한 번 무너졌다 (2026-08-14, v0.9)

소개형의 재료가 얇자 정보나루 이용분석(대출 횟수·독자 연령대·연관 키워드)을 사실파일에
채워 넣었다. 모델은 그것을 **작품 내용으로 여기고 낭독**했다 — "2025년 8월 대출 241회",
"30대 여성 독자가 꾸준히", "KEI 도서관 CB 1999". 문장에 세계가 없으니 키프레임도
빈 사무실·빈 창가만 나왔다. **책 '에 대한' 영상이 되어 버렸다.**

바로잡은 방식:

1. **도서관 통계·키워드를 사실파일에서 제거.** 재료가 얇으면 통계로 때우지 말고 만들지 않는다.
2. **소개형 규칙을 세계 중심으로 재작성.** 배경 장소·계절·시간대, 그 안의 사물과 빛과 소리.
   인물은 이름 없이("한 남자"), 사건 나열 대신 한 순간에 머문다.
3. **메타 어휘를 기계가 잡는다**(`meta_issues`). 책·소설·작가·출판사·수상·대출·도서관·표지 등이
   마지막 문장 외에 나오면 그 문장을 다시 쓴다. 규칙만으로는 새어 나왔다.
4. **세계 단서 하한**(`world_material`). 장소·계절·사물 단어를 세어 고전 4개·현대 5개 미만이면
   큐에 넣지 않는다. 실측 — 사람이 쓴 고전 사실파일 5~8, 위키 줄거리를 받은 고전 10~14,
   홍보문뿐인 현대서 1~3.

교훈: **재료가 없으면 만들지 않는 것이 맞다.** 얇은 재료를 메타데이터로 채우면
파이프라인은 정상 동작하면서 잘못된 물건을 대량으로 찍어낸다.

### 사용자 액션 필요

1. **정보나루 인증키**: data4library.kr 회원가입 → 인증키 신청 (무료, 즉시~1일)
2. ~~KEI 도서관 API URL~~ ✅ 해결 — 번들 분석으로 Pyxis API 직접 확인, Playwright 불필요.
   실측: `GET /pyxis-api/1/collections/{1|10}/search?all=k|a|검색어`. 인기대출 톱10 중 7권 KEI 소장 확인.
3. (선택) 알라딘 TTB 키

---

## 장면 자산 체계 (v1.1 계획) — 인물·배경·상황을 파일로 고정한다

v1 의 트리트먼트는 **이야기 줄기**를 세웠지만, 시각 일관성은 앵커 한 줄에 의존한다.
그 결과 같은 주인공이 장면마다 다른 얼굴·옷으로 나온다(실측: 다락방 남자 → 책 읽는 노인).
해결은 등장 요소를 **자산 파일**로 떼어 모든 장면 프롬프트에 바이트 동일하게 재사용하는 것.

### 파일 구조 (작품별)

```
output/<work>/assets/
  person1.md   주인공 시트 — 조사 기반: 원작에 묘사가 있으면 그대로, 없으면 시대·신분 고증.
               나이·체형·얼굴 특징·머리·옷차림(시대 고증)·기본 표정·소지품.
               맨 끝 ```prompt 펜스에 영어 앵커 문단(20~35단어) — 기계는 이것만 읽는다.
  person2.md   조연 (최대 2명 — 대본 규칙 A2 와 일치)
  bg.md        시대상·장소·계절·날씨·조명·분위기·팔레트. 같은 형식의 영어 앵커.
  scene<N>.md  장면별 상황 — 등장 인물(persona 참조), 행동, 카메라. 트리트먼트 beats 에서 생성.
```

### 조합 규칙

장면 프롬프트 = `화풍 블록 + scene<N>.prompt + 등장 인물의 person*.prompt + bg.prompt`.
인물·배경 문단은 **생성이 아니라 인용**이므로 장면 간에 흔들리지 않는다.
(고래 종이 장면마다 바뀌던 문제를 스타일 블록 바이트 고정으로 잡았던 것과 같은 원리.)

### 조사 단계

- 인물: 사실파일 + 웹 검색(treatment.py 의 web_snippets 재사용)으로 원작 묘사·신분·직업을
  모으고, 시대 복식으로 번역한다 (예: 1936 경성 지식인 → 낡은 두루마기 또는 해진 양복,
  창백한 낯, 부스스한 머리). **원작에 근거 없는 신체 특징은 시대 고증 범위에서만 지어낸다**
  — 이것은 각색이지 사실 왜곡이 아니다(고전 라인 한정).
- 배경: 시대상 조사 — 거리 풍경, 교통수단, 간판 없는 화면 규칙 유지, 조명(가스등/백열등),
  날씨는 이야기 줄기의 정서를 따른다.

### 일관성 2층: 참조 편집

자산 문단으로도 얼굴이 흔들리면, **첫 키프레임에서 잘 나온 인물을 Qwen-Image-Edit
참조(단독 참조)로 후속 장면에 이식**한다 — 모비딕 고래 통일에서 검증된 경로(gen_edit.py).
장당 ~15분이라 야간 창에서만 쓴다. 조건: 자가점검에서 인물 장면이 3장 이상일 때.

### 구현 순서

1. `scripts/build_assets.py` — 트리트먼트 직후 자산 파일 생성 (조사 포함, 사람 검수 가능한 md)
2. `scene_prompt.py` 가 자산 조합식으로 전환 (treatment.visual + person + bg)
3. 야간 배치 ①-0 뒤에 자산 단계 삽입, 산출물 검증(영문·한글 금지·인물 ≤2)
4. 참조 편집 자동화 (인물 등장 장면 검출 → 첫 장면을 참조로 후속 편집)
5. 관리자 페이지에 자산 탭 — person1.md 를 사람이 다듬고 재생성 없이 재사용

### 이 구조가 맞는 이유

- **재사용 = 일관성**: 인물 문단이 한 곳에 있으니 고치면 전 장면에 반영된다.
- **사람이 개입할 자리가 생긴다**: md 파일 하나 고치는 것으로 화면 전체를 바꾼다.
- **조사와 생성이 분리된다**: 조사(웹 검색·고증)는 작품당 1회, 생성은 그 결과를 인용만 한다.

## Next.js 관리자 페이지 — 언제, 무엇을

**착수 시점: P3 = 재부팅 후 첫 A/B 업로드 직후.** 지금이 아닌 이유와 그때인 이유:

- 지금 하네스·works 스키마가 이번 개편으로 바뀌는 중 — UI가 붙을 API 표면이 오늘 확정됨
- 검수 게이트의 필요성은 이미 실증됨 ("던진 사과→남기신 사과" 의미 왜곡은 기계 검증 불가,
  도서 API 시리즈의 승인 게이트도 이 페이지가 담당)
- A/B 1회는 파일 수동 업로드로 충분 — UI를 기다릴 이유가 없음

**v1 범위 (예상 1~2일):**

| 화면 | 기능 |
|---|---|
| 작업 큐 | jobs 목록·상태·진행률 (backend/db.py 이미 존재) |
| 대본 검수 | 문장별 표시 + 검증 결과 배지 + **인라인 수정** + 승인/재생성 |
| 키프레임 갤러리 | 장면별 이미지 + 승인/재생성(시드 변경)/참조 편집 요청 |
| 완성본 | 플레이어 + 다운로드 + 업로드 큐 등록 |
| 작품 관리 | works CRUD (제목·사실·스타일 블록·어미 플랜) + 도서 API 유입분 승인 |

**스택**: Next.js(:3001, Node 20 설치 완료) + FastAPI(:8000) + 기존 SQLite.
UI·cron·에이전트가 **같은 REST API**를 쓰는 것이 설계 원칙 (n8n식 동작의 본질).
systemd 유닛 2개 추가로 재부팅 영속화.

---

## 지표 루프 (P5)

업로드마다 기록: 구조 버전 태그, VVSA, 초당 리텐션 곡선, S5 도달률, 루프율.
서사 구조 효과를 격리 측정한 공개 데이터는 없으므로 — 우리 로그가 곧 근거가 된다.

---

## 관리자/공개 페이지 분리 — 미착수 명세 (2026-08-13 요청)

현재 `/`가 관리자 화면이다. 아래대로 재구성한다.

### 라우팅
- `/` — **공개 페이지**. 방문자용. 상단 메뉴로 `영상` · `인기대출도서` 전환(토스식 상단 네비, 현행 스타일 유지)
- `/admin` — 현행 관리자 화면을 그대로 이동

### 공개 페이지
- 작품당 **최신 완성본 하나만** 노출 (`kind==="final"` 이면서 `compressed` 제외, `updated` 최신)
- 상단 **캐러셀** — 5초 자동 회전, 재생 중에는 정지, 점 인디케이터로 수동 이동
- 카드 클릭 시 **모달 팝업 재생** — 배경 딤, ESC/바깥 클릭 닫기, `playsInline`·`preload="metadata"`로 부드럽게
- 인기대출도서는 하단 나열이 아니라 **별도 탭**

### 관리자 페이지 (`/admin`)
- 완성본 탭에서 **종류·버전 구분** — 백엔드가 `kind`(`final`/`scene`/`other`)와 `version`(`v3.2` 등)을 이미 내려준다.
  현재 카프카가 9개를 구분 없이 보여주는 문제가 이것으로 해소된다. 장면 클립은 접어두고 완성본만 기본 노출
- 버전 목록에 **최종 수정 타임스탬프** 표기 — `/api/works`의 `updated`(ISO 8601 UTC) 사용
- 45초 최종본만 예외적으로 버전 표기 없이 대표로 노출

### 백엔드 (완료됨)
- `GET /api/works` → `updated` 추가
- `GET /api/works/{work}/videos` → `kind`·`version`·`updated` 추가, 완성본 우선 정렬

---

## 야간 상시 제작 (강제 명세) — 20:00 ~ 익일 08:00

**GPU는 근무시간 외에 반드시 일하고 있어야 한다.** 하루 12시간 × 클립 7.3분이면
이론상 **약 98클립 = 완성본 12편**이다. 이 창을 비우는 것은 자원 낭비로 간주한다.

### 규칙
1. **20:00 기동.** `keiwi` 스타일 systemd 타이머 2종(`aivideo-night.timer` 기동,
   `aivideo-night-stop.timer` 강제 정지)으로 강제한다. cron이 아니라 systemd인 이유는
   재부팅 후 자동 복구와 `journalctl` 로그가 표준으로 붙기 때문이다.
   타이머·스크립트 모두 `Asia/Seoul` 을 명시한다 — 서버 시계가 UTC라 지정하지 않으면
   20:00 이 한국시간 새벽 5시가 된다(실측 사고).
2. **작업 단위는 작품 1편**이며, 큐가 비면 즉시 종료한다(빈 루프 금지).
3. **착수 시각과 마무리 시각을 분리한다.**
   - `START_BY=08:00` — 이후에는 **새 작품을 시작하지 않는다**.
   - **이미 착수한 작품(대본이 있는 작품)은 08:00 을 넘겨도 끝까지 마친다.** 중간에 끊으면
     그때까지 만든 키프레임·클립이 놀고, 다음 밤에 같은 자리를 다시 밟는다.
   - `FINISH_BY=09:40` — 다른 이용자가 10시쯤 서버를 쓰기 시작하므로, 이후에는
     새 장면·새 클립도 시작하지 않고 진행 중인 클립만 끝낸다.
   - `aivideo-night-stop.timer` 09:50 은 최후의 안전장치다(`SIGTERM` 후 유예 900초).
     정상적으로는 스크립트가 스스로 먼저 끝난다. 두 값은 환경변수로 덮어쓸 수 있어
     개발 중 수동 실행에는 `START_BY=1930 FINISH_BY=1930` 처럼 풀어 쓴다.
4. **정지 신호를 받아도 생산 요약을 남긴다.** `SIGTERM` 트랩으로 종료 사유·완성 편수를
   `logs/night/summary.tsv` 에 기록한다. 트랩이 없으면 강제 종료된 밤은 기록이 통째로 빈다.
4. **실패한 작품은 건너뛰고 다음으로 간다.** 한 작품의 실패가 밤 전체를 멈추면 안 된다.
5. **산출물은 자동 공개하지 않는다.** 야간 배치는 `output/`까지만 만들고,
   `deliverables/` 승격과 업로드는 사람이 관리자 페이지에서 승인한다(§검수 게이트).
6. **로그는 `logs/night/<날짜>.log`에 남긴다.** 아침에 무엇이 돌았는지 한 파일로 확인된다.

### 큐 정의
`works/queue.txt` — 한 줄에 작품 하나. 탭 5칸:
`작품ID<TAB>사실파일경로<TAB>마무리문구<TAB>화풍블록<TAB>톤`.
4·5칸은 비워도 되며 각각 기본 화풍(동판화)과 `담담` 톤이 쓰인다.
톤은 `scripts/tts_render.py` 의 6종(담담·따뜻·서늘·긴장·속삭임·생동)이고,
사실파일에 `[톤] 생동` 줄이 있으면 그쪽이 우선한다(문장 어휘와 발화가 같은 값으로 움직인다).
작품ID는 파일명·ComfyUI 입력명이 되므로 ASCII 여야 한다 —
`scripts/fetch_book_facts.py` 의 `slug()` 가 한글 제목을 로마자로 옮긴다.
처리한 줄은 `works/done.txt`로 옮긴다. 파일 기반인 이유는 워커 미구현 상태에서도
즉시 동작하고, 나중에 SQLite 큐(`backend/db.py`)로 옮길 때 인터페이스가 같기 때문이다.

### 확인
```bash
systemctl list-timers 'aivideo-night*'        # 다음 기동 시각
journalctl -u aivideo-night -n 50             # 지난밤 실행 기록
tail -f logs/night/$(date +%F).log            # 진행 중 확인
```
