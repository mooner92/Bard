# 변경 이력

## v1.0.0 (2026-08-14)

첫 정식 릴리스. 합격 기준은 [docs/SPEC.md](docs/SPEC.md).

### 이번 릴리스의 핵심

**책 '에 대한' 이야기가 아니라 책 '속' 세계를 그린다.** 직전 개발판(v0.9)이 자동화 과정에서
작품 세계 대신 도서관 통계를 낭독하는 영상을 만들었고, 그것을 바로잡는 것이 v1 의 중심이다.

### 추가

- **고전 라인** — `works/classics.txt` 원장 + 위키백과 줄거리·배경 자동 수집
  (`refill_queue.py --classics`). 퍼블릭 도메인이라 각색이 자유롭고 세계를 그릴 재료가 충분하다.
- **세계 단서 하한**(`world_material`) — 장소·계절·사물 단어를 세어 고전 4·현대 5 미만은
  제작하지 않는다. 재료가 없으면 만들지 않는 것이 맞다.
- **메타 어휘 검출**(`meta_issues`) — 책·작가·출판사·수상·대출·도서관 등이 본문에 나오면 다시 쓴다.
- **지시어 누출 검출**(`leak_issues`) — 프롬프트 지시어와 모델의 해명이 대본이 되는 사고를 막는다.
- **사실 대조**(`source_issues`) — 지어낸 고유명사를 모델에게 판정만 시키고, 오탐을 두 겹으로 거른다.
- **말의 온도**(`tts_render.py`) — 톤 6종 × 텐션 곡선 4종 × 낱말 강세. 한국어 Azure 음성은
  감정 스타일을 지원하지 않아 prosody·break·emphasis 조합으로 만든다.
- **자가점검**(`review_output.py`) — 길이·감속·음량·리듬·대본을 재고 `logs/review/` 에 남긴다.
  음량이 벗어나면 `--fix-loudness` 로 −14 LUFS 재교정(영상 트랙은 무손실 복사).
- **KEI 도서관 소장 조회**(`kei_holdings.py`) — Pyxis JSON API 직접 사용, 24시간 캐시.
- **관리자 점검 탭** — 판정 기준은 서버가 갖고 화면은 표시만 한다.
- **명세 대조 테스트**(`tests/test_spec.py`) — GPU·외부 API 없이 수초 안에 확인.

### 변경

- 야간 배치가 **착수(08:00)와 마무리(09:40)를 분리**한다. 8시 전에 시작한 작품은 끊지 않고 끝낸다.
- 정지 신호(SIGTERM)를 받아도 생산 요약을 남기고, 자식 프로세스까지 함께 정리한다.
- 클립 프레임 수를 4n+1 로 **올림**한다(내림하면 감속이 1.5배를 살짝 넘었다).
- 최종 인코딩에 `loudnorm=I=-14` 를 넣는다(이전 산출물은 −17 LUFS 로 조용했다).
- 문장 길이 29~40자, 마지막 문장은 마무리 문구 길이 + 12자까지 허용.
- 승인 전 야간 산출물은 `kind: "night"` 으로 분류해 **공개 페이지에 나오지 않는다**.

### 고침

- 타이머·스크립트에 `Asia/Seoul` 명시 — 서버 시계가 UTC 라 20:00 이 한국시간 새벽 5시에 걸렸다.
- 프롬프트의 길이 지시와 검증기 상한이 서로 달라 싸우던 문제(22~42 vs 22~33).
- 위키백과 동음이의 오염 — '모순' 검색이 논리학 문서를 물어왔다. 저자명 확인 후 채택한다.
- 자료 대조 수리가 마지막 문장의 마무리 문구를 지우던 문제.
- 강세 표시가 스타일·수리 패스에서 사라지던 문제, 짝이 맞지 않던 문제.
- 정보나루 상세 API 가 `Accept: application/json` 에 406 을 반환하던 문제(예외를 삼켜 자료 부족으로 오인).

### 알려진 제약

- 8문장 자동 생성은 문체 규칙(어미 계열·길이)까지 완전히 수렴하지는 않는다. 사실 오류가 아닌
  문체 잔여는 아침 검수에서 다듬는다.
- 화면 상단·하단의 가짜 자막은 사라졌지만(영문 프롬프트 전환), 벽에 걸린 족자·현판 같은
  **화면 안쪽 글자류는 남을 수 있다.** 크롭으로 못 잡는 위치라 프롬프트·네거티브에 의존한다.
- 장면 묘사가 길어지면 지정한 화풍에서 벗어나는 경우가 있다(1930년대 경성 수묵 → 사진풍).
  화풍 블록은 프롬프트 앞머리에 바이트 동일하게 들어가지만 완전히 고정되지는 않는다.
- 현대 라인은 세계 단서가 충분한 책이 드물어 실제 제작 편수가 적다.
